# SPEC.md — くりや帖(kuriya-cho)

<!-- scaffold template v1.30.0 から展開(2026-09-05)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

1814 年にパリで出た料理書 —— A. B. ボーヴィリエ『料理人の技法(L'Art du Cuisinier)』第一巻を、
**いま読める一枚のカードに変える**。

原文は 200 年前のフランス語である。読める形にするには、綴りを現代に寄せ、材料と手順を
見分け、日本語を添える必要がある。この三つは技術としては別物だが、
**利用者に見えるのは「昔の本が読めるようになった」という一つのことだけ**にする。

**ただし、綴りの遠さについての当初の見込みは実測で壊れた**(loop_002・§8 を参照)。
壁は綴りではなく語彙の側にあった。アプリの重心は和訳と用語集に置く。

著者は革命前にプロヴァンス伯(のちのルイ 18 世)の officier de bouche を務め、
1782 年にリシュリュー通りで「ラ・グランド・タヴェルヌ・ド・ロンドル」を開いた
——「パリ最初の本格的なレストラン」とされる人物である。この本は、宮廷の厨房の技術が
革命を経てレストランという新しい場に移し替えられる、その転換点に立つテキストとして読める。

## 2. 素材と権利

| 素材 | 出所 | 扱い |
|---|---|---|
| 本文 | Project Gutenberg #64976(2021-03-31 公開 / 2024-10-18 更新)。底本は BnF/Gallica の画像 | パブリックドメイン。`data/raw/` にキャッシュし sha256 を `SOURCES.json` に記録 |
| 現代フランス語の語彙集 | `words/an-array-of-french-words`(336,524 語)/ `Taknok/French-Wordlist`(22,740 語)。いずれも Grammalecte/Dicollecte 系 | 正規化の**外部オラクル**。出荷物には同梱しない |
| 和訳 | 本プロジェクトで生成し、リポジトリに焼き込む | 実行時に翻訳 API を呼ばない(課金経路ゼロ) |

第二巻(tome II)は Project Gutenberg に無い。**本アプリの対象は第一巻だけ**であり、
画面にもそう明示する。

## 3. 機能要求

| ID | 要求 | 優先度 | 状態 |
|---|---|---|---|
| F-01 | 素材を取得し、ハッシュつきでキャッシュする | must | ✅ loop_001 |
| F-02 | 本文を章とレシピに分割する。目次を件数オラクルとして突き合わせる | must | ✅ loop_001 |
| F-03 | 巻頭の献立(services de table)を献立・組・皿に分割する | should | ✅ loop_001 |
| F-04 | 1814 年の綴りを現代綴りへ正規化する。規則と例外辞書を分ける | must | ✅ loop_002 |
| F-05 | レシピ本文から材料・道具・分量・火・工程を見分けて色分けする | must | ✅ loop_003 |
| F-05b | 本文の相互参照を検出して解決し、たどれる形にする。切れた参照も出す | should | ✅ loop_003 |
| F-06 | 料理名を全件和訳する。用語集で料理語を固定する | must | |
| F-07 | レシピ本文を和訳する。充填率を分子と分母で表示する | must | |
| F-08 | 材料から引く(材料チップ + 自由入力 → カード一覧) | must | |
| F-09 | 架空の一皿を作る(本書の料理名の文法から。決定論・seed 指定) | should | |
| F-10 | 入口にビフォー/アフター(1814 年の版面 → カード)を置く | must | |
| F-11 | 内部の指標(規則の被覆・抽出の被覆・突合の結果・落ちたゲート)を「仕組みを見る」に隔離する | must | |
| F-12 | フリート共通フッタ(5 項目・下部固定) | must | |

## 4. 品質ゲート

**宣言しただけのゲートは誰も守らない**(HC-157)。各 `G-xx` は TEST_SPEC のケースから
ID で参照されるか、ここに「未実装」と明記されているかのどちらかでなければならない。

| ID | ゲート | 判定 | 状態 |
|---|---|---|---|
| G-01 | **件数オラクル**: 巻末の目次と本文抽出の突合で、説明のつかない食い違いが 0 件 | `pg_parse.reconcile()["unexplained"] == []` | ✅ 実測 2026-09-05: 29/32 章一致・未説明 0 |
| G-02 | **数詞オラクル**: 献立の組見出しの数詞と皿数の合計が一致(既知の 2 件を除く) | `menus.check()["mismatched"]` が既知集合と一致 | ✅ 実測: 79 組中 77 一致 |
| G-03 | **消費率**: 原文の全行がどれかの出力単位に帰属する(残差 0) | `coverage.body_coverage()` / `menus_coverage()` | ✅ 実測: 本文 9,852 行・献立 715 行とも 1.00000 |
| G-04 | **正規化の非循環オラクル**: 正規化後の語が現代フランス語の語彙集に載る割合が、正規化前より高い。正規化器は語彙集を実行時に引かない(検査済み) | `spelling_audit.audit()` | ✅ 実測 2026-09-05: 0.8652% → 0.4333%(360 箇所を置換) |
| G-05 | **正規化の陰性対照**: 規則を現代語彙集 344,060 形すべてに当てて壊れる語 0。例外辞書の置換先がすべて現代語に実在 | `spelling_audit.rule_collateral()` | ✅ 実測: 採用規則 0 件破壊 / 不採用の `-ans→-ants` は 726 語中 723 語を破壊 |
| G-06 | **抽出の被覆**: 材料・工程のスパンが全レシピに 1 つ以上付く。付かないレシピを列挙でき、その理由が言えること | `extract.coverage()` | ✅ 実測 2026-09-05: 材料 620/628・工程 611/628・語の被覆 34.18%。付かない 8 件はすべて参照だけの項 |
| G-13 | **参照の解決**: 本文の相互参照が表題に解決すること。解決しないものは名前を出す | `crossref.audit()` | ✅ 実測: 307 件中 234 件解決(76.2%)。解決しない 73 件のうち 5 つは第一巻に無い章 = 第二巻 |
| G-07 | **和訳のタグ保存**: 原文に付けたスパン ID が訳文にすべて残る。崩れた文は原文のみ表示へ落とす | 未実装(F-07) | |
| G-08 | **字種検査**: 和訳本文にキリル文字・ギリシア文字・不可視文字が混入しない(フリート共通の罠) | 未実装(F-06/F-07) | |
| G-09 | **循環の禁止**: 検索の索引とカードの表示が同じ前処理を共有しない。索引の正しさを索引で測らない | 未実装(F-08) | |
| G-10 | **生成器の決定論**: 同じ seed で同じ一皿。生成した料理名が本書に実在しないこと | 未実装(F-09) | |
| G-11 | **図の収まり**: 図の中の要素が `viewBox` に収まる(`getBBox()` で測る) | 未実装 | |
| G-12 | **横溢れ**: どの画面も本文が横スクロールしない(実ブラウザで測る) | 未実装 | |

## 5. 画面

| 画面 | 入力 | 出力 |
|---|---|---|
| 入口 `index.html` | スライダー 1 本 | 1814 年の版面 ⇄ 現代のカード。文字で説明せずに「何をするアプリか」を見せる |
| 探す `sagasu.html` | 材料チップ + 自由入力 | カードの一覧。各カードに料理名(原題+和名)・分類・代表材料・**なぜ出たかの一行**。類似度の数値は出さない |
| 読む `yomu.html` | カードの選択 + 綴りトグル | 三層(1814 年の原文 / 現代の綴り / 日本語)。既定は日本語。色分けは原文で取り、タグで訳文へ引き継ぐ |
| 卓 `taku.html` | 献立の選択 | 13 の献立と 488 の皿。組ごとの皿数と、巻末の卓図での位置 |
| 網 `ami.html` | なし | 本が自分を参照する網。解決した 234 本と、たどれない 73 本(うち 5 章は第二巻) |
| 遊ぶ `asobu.html` | 種類 + スライダー | 架空の一皿。スライダーは「まじめ ⇄ 気まぐれ」 |
| 仕組み `shikumi.html` | なし | 内部の指標と、**落ちたゲート・捨てた主張**をそのまま置く |

### 画面に出す言葉(利用者の語に置き換える)

| 内部の呼び名 | 画面上の表示 |
|---|---|
| 正規化 | 現代の綴りで読む |
| スパン抽出 | 材料と手順を色分け |
| 索引・類似度 | 材料から探す |
| 温度パラメータ | まじめ ⇄ 気まぐれ |
| 信頼度 | (数値は出さない。細い横バーのみ) |

## 6. 誠実さの規約

- **機械生成である旨は必ず出す。** 目立たせすぎず、しかし省かない
- **原文へ戻る導線を常に画面内に残す。** 三層トグルはどの層からも 1 手で原文に戻れる
- **当時の単位は「約」を外さない。** `verre` / `cuillerée` は地域と年代で幅がある。
  換算値を断定形で書かない
- **測って落ちた主張は消さずに残す。** 「仕組みを見る」に、落ちたゲートと捨てた見込みを置く

## 7. スコープ外

- 第二巻(PG に無い)
- 実行時の API 呼び出し(翻訳・生成とも。**課金経路をゼロにする**)
- レシピを実際に作るための分量指示としての利用(料理史の資料として読ませる)
- ブラウザ内でのモデル学習・推論(本作は決定論的な規則と索引だけで組む)

## 8. 測って壊れた見込み

**残す。** 外れた予測を消すと、後から読んだ人が同じ見込みで同じ設計をする。

| 見込み(いつ書いたか) | 実測 | どうしたか |
|---|---|---|
| 「1814 年の綴りが読解の壁である」(loop_001 の SPEC §1) | 本書の地の文 82,987 照合形のうち、現代フランス語の語彙集に載らないのは **718 = 0.87%**。同じジャンルで綴りが現代の対照(PG #6966・1896 年序)は **998 / 61,440 = 1.62%** で、**1814 年の本より遠い** | 綴りの層は残すが、アプリの重心を**語彙**(和訳・用語集)へ移した。数字は「仕組みを見る」に出す |
| 「`étoit` → `était` の半過去が頻出する」(通説) | この本には**無い**。命令形で書かれた料理書なので直説法半過去が出てこない | 規則を書かなかった |
| 「章への参照はレシピ表題に部分一致させれば解決できる」(loop_003 の実装当初) | `ENTREMETS` が別のレシピ表題に当たって「解決した」ことになり、**第二巻を指しているという事実が消えた**。章の参照は章の名だけに当てるべきだった | 大文字組みの参照は章名だけに解決するよう分けた。誤って解決すると発見そのものが消える型の失敗として T-133 に固定 |
| 「`-ans` → `-ants` は規則で書ける」(教科書的な記述) | 現代語彙集の `-ans`/`-ens` 語 726 語に当てると **723 語が語でなくなる**(`anciens` → `ancients`)。規則として書けない | 本書に出る 15 語の**閉じた表**にした。不採用の規則も陽性対照としてテストに残す(T-124) |
